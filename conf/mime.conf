# Mime types added by the asciinema plugin.
#
# This file is appended to the mime configuration cascade by action.php, which
# makes .cast recordings uploadable through the media manager and servable
# through lib/exe/fetch.php. See https://www.dokuwiki.org/mime
#
# Set the "Register the cast mime type" option of the plugin to off if you
# prefer to declare the extension yourself in conf/mime.local.conf.

cast	application/x-asciicast
